"use client";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
import { SiteHeader } from "../../components/SiteHeader";
export default function AdminLogin(){const [error,setError]=useState("");const [busy,setBusy]=useState(false);const router=useRouter();async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setError("");const data=new FormData(event.currentTarget);const response=await fetch("/api/admin/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email:data.get("email"),password:data.get("password")})});if(response.ok){router.push("/admin");router.refresh()}else{const body=await response.json().catch(()=>({error:"Login failed."}));setError(body.error||"Login failed.");setBusy(false)}}return <main className="login-page"><SiteHeader/><section className="login-card"><a href="/" className="brand"><b>P</b>Paperly</a><p className="kicker">OWNER ACCESS</p><h1>Admin sign in</h1><p>Use the private Paperly admin credentials. This page is intentionally not linked anywhere on the public website.</p><form onSubmit={submit}><label>Email<input name="email" type="email" autoComplete="username" required/></label><label>Password<input name="password" type="password" autoComplete="current-password" required/></label>{error&&<div className="login-error" role="alert">{error}</div>}<button className="primary" disabled={busy}>{busy?"Signing in...":"Sign in"}</button></form><a href="/" className="back-home">Back to Paperly</a></section></main>}
