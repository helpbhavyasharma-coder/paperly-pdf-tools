import { SiteHeader } from "./SiteHeader";import { SiteFooter } from "./SiteFooter";export function InfoPage({eyebrow,title,intro,children}:{eyebrow:string;title:string;intro:string;children:React.ReactNode}){return <main><SiteHeader/><header className="page-hero compact-page-hero shell"><p className="kicker">— {eyebrow}</p><h1>{title}</h1><p>{intro}</p></header><section className="info-content shell">{children}</section><SiteFooter/></main>}
