import type { Metadata } from "next";
export const metadata:Metadata={title:"Terms of Use | Paperly",description:"Read the terms for using Paperly's free browser-based PDF tools responsibly.",alternates:{canonical:"/terms"}};
export default function Layout({children}:{children:React.ReactNode}){return children;}
